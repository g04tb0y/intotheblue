# Constraint: requires Python 3.10 (pc-ble-driver-py has no wheel for Python >= 3.11).
# Create the environment with:  uv venv --python 3.10 .venv310
blatann==0.6.0
PyYAML==6.0.3
# pc-ble-driver-py==0.17.0 is installed automatically as a blatann dependency.
